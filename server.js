console.error("O servidor SQLite local foi isolado em legacy/server-sqlite.js.");
console.error("Use `npm run dev` para rodar o CRM com o mesmo fluxo da Netlify.");
console.error("Se precisar consultar o legado, use `npm run legacy:sqlite`.");
process.exit(1);
